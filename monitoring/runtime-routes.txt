# Runtime route contract for smoke, monitoring, and incident tooling
# Format:
# GET <path>
# REDIRECT <path> <expected-location>
# Smoke also enforces that these routes stay backed by export artifacts,
# sitemap coverage, or explicit nginx redirect/health rules.

GET /
GET /about
GET /services
GET /contact
GET /robots.txt
GET /sitemap.xml
GET /health
